kafka:
  bootstrapAddress: ${spring.embedded.kafka.brokers}
  groupId: products
  topic: products

elasticsearch:
  index: store_products
  server: ${embedded.elasticsearch.host}:${embedded.elasticsearch.httpPort}


spring:
  cloud:
    config:
      enabled: false
  sleuth:
    sampler:
      percentage: 1.0
  zipkin:
    sender:
      type: web