apiVersion: apps/v1
kind: Deployment
metadata:
  name: search-orders-service
spec:
  selector:
    matchLabels:
      app: search-orders-service
  replicas: 1
  template:
    metadata:
      labels:
        app: search-orders-service
        collect_logs: "true"
    spec:
      containers:
        - name: search-orders-service
          image: gcr.io/complete-tube-306819/search-orders-service:1.0.0
          imagePullPolicy: Always
          ports:
            - containerPort: 8080
          livenessProbe:
            initialDelaySeconds: 15
            httpGet:
              port: 8080
              path: /actuator/health/liveness
          readinessProbe:
            initialDelaySeconds: 15
            httpGet:
              port: 8080
              path: /actuator/health/readiness
          resources:
            limits:
              memory: 512Mi
            requests:
              cpu: 100m
              memory: 128Mi


---
apiVersion: v1
kind: Service
metadata:
  name: search-orders-service
  labels:
    collect_logs: "true"
spec:
  type: ClusterIP
  selector:
    app: search-orders-service
  ports:
    - port: 8080
      targetPort: 8080