spring:
  application:
    name: promotion-service
  cloud:
    config:
      discovery:
        service-id: config-service
  zipkin:
    base-url: http://localhost:9411
  sleuth:
    sampler:
      probability: 1

feign:
  hystrix:
    enabled: true


