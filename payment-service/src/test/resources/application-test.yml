spring:
  cloud:
    config:
      enabled: false

  kafka:
    producer:
      bootstrap-servers: ${spring.embedded.kafka.brokers}
      client-id: orders
      acks: all
      retries: 3
      key-serializer: org.apache.kafka.common.serialization.StringSerializer
      value-serializer: org.springframework.kafka.support.serializer.JsonSerializer
      properties:
        max-in-flight-requests-per-connection: 1
        transaction-timeout-ms: 1000
        request-timeout-ms: 1000
        retry-backoff-ms: 1000
kafka:
  bootstrapAddress: ${spring.embedded.kafka.brokers}
  consumer:
    topic: orders
    groupId: paymentGroup
  producer:
    topic: catchup-orders

stripe:
  url: https://api.stripe.com
  public-key: sk_test_51HYJubArLuFLaKsZh7FrusGK6heQuk5nXZMBUQTjKvOX1X2eQWmBa2r3b2Y1DoW1gZ1gySQqmhulgEPKL8iS4b0900FjjO3TLL

order:
  url: http://localhost:8086
  redirect: http://localhost:3000/processing

logging:
  config: classpath:logback-spring-test.xml
